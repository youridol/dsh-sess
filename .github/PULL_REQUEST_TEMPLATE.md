## What does this change?

<!-- One paragraph: the behavior change and why it is safe on the dsh-v0.1.2-alpha.5 official API. -->

## Compliance

- [ ] Uses only official DSH services / endpoints / bundles / patches / Cordis / client slots
- [ ] No harness source edits, private APIs, monkey patches, DOM hacks or version branches
- [ ] Behavior boundaries documented (cold-only deletion, no unarchive, subagent sessions, orphans)
- [ ] Unit tests added/updated for host ops or pure browser logic
- [ ] zh/en copy keys kept in sync (`src/client/locales.ts`)
- [ ] `npm run check` passes locally

## Tests

<!-- Which suites cover this change? -->
